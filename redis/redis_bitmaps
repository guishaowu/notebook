# bitmaps 新的数据结构，实质是string
一个由很多bit构成的数据，处理bool类型
操作
setbit key  10  1
getbit key 10 
bitcount 

使用场景  实时分析，布尔类型高效存储

避免位数太多，可以分为多个key  bit-1,bit-2  (每个M位)

举例说明：
        统计网站用户每天访问的最长连续时间
从第零天开始，用户访问网站后，设置对应的位为1,可以使用很短的位记录用户访问信息
